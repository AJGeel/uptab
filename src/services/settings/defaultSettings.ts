import { Settings } from "./types";

export const defaultSettings: Settings = {
  sidebar: {
    showShortlinks: true,
    showBookmarks: true,
    showUpdates: true,
    showInfoWidget: true,
    showFeedbackPrompt: true,
    isOpen: false,
  },
  homescreen: {
    showInfoWidget: true,
  },
};
