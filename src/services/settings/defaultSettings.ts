import { Settings } from "./types";

export const defaultSettings: Settings = {
  sidebar: {
    showShortlinks: true,
    showBookmarks: true,
    showUpdates: true,
    showInfoWidget: true,
  },
  homescreen: {
    showFeedbackPrompt: true,
    showInfoWidget: true,
  },
};
